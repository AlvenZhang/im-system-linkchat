# 服务器配置
server.port=8080
server.servlet.context-path=/api

# 数据库配置
spring.datasource.url=jdbc:mysql://115.190.228.176:3306/linkchat?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
spring.datasource.username=alven
spring.datasource.password=alven_is_root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# MyBatis配置
mybatis.mapper-locations=classpath:mapper/*.xml
mybatis.type-aliases-package=com.linkchat.entity
mybatis.configuration.map-underscore-to-camel-case=true

# Redis配置
spring.data.redis.host=115.190.228.176
spring.data.redis.port=6379
spring.data.redis.password=
spring.data.redis.database=0

# JWT配置
jwt.secret=linkchat_secret_key_2024
jwt.expiration=3600000
jwt.header=Authorization

# 日志配置
logging.level.com.linkchat=INFO
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n
# CORS配置
spring.mvc.cors.allowed-origins=*
spring.mvc.cors.allowed-methods=GET, POST, PUT, DELETE, OPTIONS
spring.mvc.cors.allowed-headers=*